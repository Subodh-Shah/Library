class Book {
	constructor(title, author, publicationYear, pages, readPages) {
		this.title = title;
		this.author = author;
		this.publicationYear = publicationYear;
		this.pages = pages;
		this.readPages = readPages;
	}
}

export default Book;
